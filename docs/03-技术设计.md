# 技术设计 v1

日期：2026-09-03
基线：《01-范围清单》《02-用例与用户流程》。本文定义技术栈、工程结构、核心数据模型、任务接口、执行引擎、原生层封装、持久化、UI 结构、构建与测试。

---

## 1. 目标与约束

| 约束 | 设计响应 |
|---|---|
| 下载即运行、体积小 | .NET Framework 4.8（Win11 自带）+ 单 exe，目标 < 10 MB |
| 高效可靠、只留核心 | 任务全部为 C# 类，无脚本；每个任务幂等，Detect 先行 |
| 可预览、可校验 | Detect → Plan → Apply → Verify 四段式；执行前展示 diff |
| 视觉简约流畅 | WPF + WPF-UI（Fluent、Mica、NavigationView 过渡） |
| 微软服务默认保留 | 去推送组默认不勾选；不含任何卸载/禁服务任务 |
| 一次提权 | manifest `requireAdministrator`，启动时校验 HKCU 归属 |

---

## 2. 技术栈

| 层 | 选择 | 版本约束 |
|---|---|---|
| 运行时 | .NET Framework 4.8，SDK 风格 csproj，`LangVersion` latest | 需 `PolySharp` 提供 `IsExternalInit`、`required` 等 polyfill |
| 构建 | .NET 10 SDK（仅构建机需要，产物不依赖） | `dotnet build`；引用程序集由 `Microsoft.NETFramework.ReferenceAssemblies` 自动拉取 |
| UI | WPF + `WPF-UI` 3.x | Mica 仅 Win11 生效，Win10 自动退化为纯色 |
| MVVM | `CommunityToolkit.Mvvm` 8.x | 源码生成器 |
| JSON | `System.Text.Json` 8.x（NuGet） | 所有模型标注 `JsonPropertyName`，snake_case |
| 原生互操作 | 手写 P/Invoke（`DllImport`），不引入 CsWin32 以减少构建复杂度 | 仅 6 个函数：`SHSetKnownFolderPath`、`SHGetKnownFolderPath`、`SHChangeNotify`、`SendMessageTimeout`、`OpenProcessToken`/`GetTokenInformation` |
| WMI | `System.Management`（框架自带） | 分区、还原点、硬件探测 |
| 日志 | 自写 `FileLogger`（按天滚动，单文件 ≤ 5 MB） | 不引入 Serilog |
| 单 exe | `Costura.Fody` 嵌入依赖 | 发布配置启用，调试配置关闭 |
| 测试 | xUnit + FluentAssertions | 仅测 Core 与 Tasks 的纯逻辑 |

---

## 3. 解决方案结构

```
NewPcSetup.slnx
src/
  NewPcSetup.Core/        模型、ITask、TaskRunner、Planner、日志、持久化。无 WPF 依赖
  NewPcSetup.Native/      注册表、已知文件夹、环境变量、Explorer、还原点、存储 WMI、进程令牌
  NewPcSetup.Tasks/       所有任务实现 + 任务目录（TaskCatalog）
  NewPcSetup.App/         WPF 入口、6 个页面、ViewModel、主题资源、软件目录 JSON
tests/
  NewPcSetup.Core.Tests/  Planner、TaskRunner、模型序列化
  NewPcSetup.Tasks.Tests/ 每个任务的 Detect/Plan 逻辑（注入伪造的 IRegistry / IEnvironment）
build/
  inspect-machine.ps1     只读采集脚本（调研用）
  publish.ps1             Release 构建 + 产物校验
docs/
```

依赖方向：`App → Tasks → Native → Core`，`Tasks → Core`。Core 不引用 Native，Native 中对外的能力以接口形式定义在 Core（`IRegistry`、`IEnvironment`、`IShell`、`IStorage`、`ISystemRestore`），Native 提供实现，测试提供伪实现。

---

## 4. 核心数据模型

全部为不可变 record，JSON 持久化到 `%ProgramData%\NewPcSetup\`。

### 4.1 EnvironmentSnapshot（探测结果）

```csharp
public sealed record EnvironmentSnapshot(
    string OsVersion, string DisplayVersion, int Build, bool IsWindows11,
    bool IsLaptop, int RamGb, string Manufacturer, string Model,
    bool IsMdmEnrolled, bool IsDomainJoined, bool ProxyEnabled,
    IReadOnlyList<DiskInfo> Disks, IReadOnlyList<VolumeInfo> Volumes,
    string SystemDrive, string? DataDrive, DateTime InstallDate,
    OneDriveInfo OneDrive, GpuInfo Gpu,
    IReadOnlyDictionary<string, string> KnownFolders,
    IReadOnlyDictionary<string, string> UserEnvironment,
    IReadOnlyList<DetectedTool> Tools,
    IReadOnlyList<LargeItem> LargeItems,
    DateTime TakenAt);
```

- `DataDrive`：非系统卷中容量最大者；无则为 null，触发分盘建议。
- `Tools`：`{ Id, Name, Installed, ConfiguredPath }`，Id 与 `env.*` 任务一一对应。
- `LargeItems`：`{ Path, SizeBytes, Category }`，Category ∈ `hiberfil | pagefile | temp | phonelink | appdata`。

### 4.2 Answers（问卷）

```csharp
public sealed record Answers(
    Usage Usage,                 // Office | Dev | Gaming | Home
    string? DataDrive,           // "D:" 或 null（单盘不分区）
    bool CreatePartition,        // 单盘时是否按建议分区
    UiStyle UiStyle,             // Win11Default | Win10Like
    ImeMode ImeMode,             // ChineseDefault | EnglishDefault
    bool KeepShiftSwitch,
    bool DisablePromotions);     // 默认 false
```

### 4.3 Plan / PlanItem

```csharp
public sealed record Plan(string Id, DateTime CreatedAt, Answers Answers,
    string SnapshotHash, IReadOnlyList<PlanItem> Items);

public sealed record PlanItem(
    string TaskId, string Module, string DisplayName,
    PlanState State,             // Skipped | Planned | NotApplicable
    bool Checked,                // 用户勾选状态（默认由任务给出）
    string? CurrentValue, string? TargetValue,
    RiskFlags Risk,              // Reversible | NeedsExplorerRestart | NeedsReboot | AdminOnly | PromptOnly
    string? Reason,              // Skipped/NotApplicable 的原因
    IReadOnlyList<string> DependsOn);
```

### 4.4 Journal / ExecutionResult

```csharp
public sealed record JournalEntry(string TaskId, DateTime At, JournalKind Kind,
    string Key, string? OldValue, string? NewValue);   // Kind: Registry | EnvVar | KnownFolder | File | Power

public sealed record TaskResult(string TaskId, TaskOutcome Outcome,  // Done | Skipped | Failed | RolledBack | NeedsReboot | ManualStep
    string? Message, TimeSpan Elapsed, IReadOnlyList<string> ManualSteps);

public sealed record ExecutionResult(string PlanId, DateTime StartedAt, DateTime? FinishedAt,
    bool Aborted, bool RebootRequired, IReadOnlyList<TaskResult> Results);
```

### 4.5 持久化路径

```
%ProgramData%\NewPcSetup\
  snapshot-<ts>.json
  plan-<ts>.json
  journal-<ts>.json        追加写，每条一行（JSON Lines）
  result-<ts>.json
  state.json               { lastPlanId, pendingResume: bool }
  logs\app-<date>.log
```

---

## 5. 任务接口

```csharp
public interface ITask
{
    TaskMetadata Metadata { get; }
    bool IsApplicable(EnvironmentSnapshot s, Answers a);          // 是否出现在方案中
    bool DefaultChecked(EnvironmentSnapshot s, Answers a);        // 默认是否勾选
    DetectResult Detect(TaskContext ctx);                          // 当前值 / 目标值 / 是否已满足
    void Apply(TaskContext ctx);                                   // 修改，所有写操作经 ctx 记 journal
    bool Verify(TaskContext ctx);                                  // 重新读取确认
    void Rollback(TaskContext ctx, IReadOnlyList<JournalEntry> entries);
}

public sealed record TaskMetadata(
    string Id, string Module, string DisplayName, string Description,
    RiskFlags Risk, IReadOnlyList<string> DependsOn, int Order);

public sealed record DetectResult(bool Satisfied, string? CurrentValue, string? TargetValue, string? Reason = null);

public sealed class TaskContext
{
    public EnvironmentSnapshot Snapshot { get; }
    public Answers Answers { get; }
    public IRegistry Registry { get; }          // 写操作自动记 journal
    public IEnvironment Environment { get; }
    public IShell Shell { get; }
    public IStorage Storage { get; }
    public IFileSystem FileSystem { get; }      // 目录创建记 journal；文件删除不可撤销
    public IPower Power { get; }                // 休眠开关，写操作记 journal
    public IJournal Journal { get; }
    public ILogger Log { get; }
    public CancellationToken Cancellation { get; }
    public IList<string> ManualSteps { get; }   // 任务可追加"手动项"进入报告
}
```

约定：
- `Detect` 不得有副作用。
- `Apply` 只做一件事；需要多步的拆成多个任务并用 `DependsOn` 串联。
- 键不存在且不适用于当前版本时，`Detect` 返回 `Satisfied=false, Reason="not_applicable"`，Planner 据此标为 NotApplicable。
- 任务 Id 采用 `模块.名称` 小写点分，与《02》任务库索引一致。

`TaskCatalog` 以静态列表注册全部任务；不做反射扫描，便于裁剪与审计。

---

## 6. Planner 与 TaskRunner

### 6.1 Planner

```
foreach task in Catalog (按 Order 排序)
  if !IsApplicable → 不进入 Plan
  d = Detect
  state = d.Reason == "not_applicable" ? NotApplicable : d.Satisfied ? Skipped : Planned
  checked = state == Planned && DefaultChecked
  emit PlanItem
```

依赖规则：父项取消勾选时，所有 `DependsOn` 指向它的子项自动取消；父项 Skipped 视为满足，不阻断子项。

### 6.2 TaskRunner

- 输入：Plan 中 `Checked && State == Planned` 的项，按 `Order` 排序并做拓扑校验。
- 每项：`Apply → Verify`；Verify 失败或 Apply 抛错 → `Rollback(journal 中该任务条目)` → Outcome = RolledBack / Failed。
- 失败不中断，继续下一项。
- `NeedsExplorerRestart` 的任务在组末统一重启一次 Explorer（Runner 收集标志，在最后一个此类任务完成后执行）。
- `NeedsReboot` 汇总到 `ExecutionResult.RebootRequired`。
- 取消：`CancellationToken` 只在任务边界检查；当前任务完成后停止，结果标 `Aborted`。
- 进度通过 `IProgress<RunnerProgress>` 回传 UI。

### 6.3 前置与收尾

Runner 之外由 `ExecutionCoordinator` 负责：创建还原点（失败仅告警）→ 写 `state.json pendingResume=true` → Runner → 写结果 → `pendingResume=false` → 若需重启且用户同意则写 `HKCU\...\RunOnce` 指向 `exe --resume`。

续跑（UC-12）：
- Runner 每完成一个任务，Coordinator 就把部分结果写到 `result-<id>.json`（`FinishedAt=null`），崩溃后不丢已完成项。
- 启动时 `LoadLast()`：`pendingResume=true` 且结果未完成 → 询问是否续跑；`Continue()` 把已有结果的任务取消勾选后重新走 Runner，结果与旧结果合并。
- `--resume`（RunOnce 拉起）且上次已完成 → `Reverify()` 对 `NeedsReboot` 项重新 `Verify`，通过改为 Done，直接进报告页。

分区（UC-05）：`disk.shrink_and_create` 只在问卷同意（`Answers.CreatePartition`）时进入方案；`Detect` 里用 `IStorage.GetSupportedSize` 复核可压缩量 ≥ 建议 D 的 80%，不满足标 NotApplicable 并给原因。`path.skeleton` 依赖它，取消分区即级联取消全部路径任务。分区不可撤销，`Rollback` 只写手动项。Storage WMI 的方法调用需要管理员（非提权进程 `GetSupportedSize` 返回 Access denied）。

---

## 7. 原生层封装

| 接口 | 实现要点 |
|---|---|
| `IRegistry` | `Microsoft.Win32.Registry`；`Set` 前读旧值写 journal；支持 `DWORD/String/ExpandString/Binary`；`DeleteValue` 同样记录 |
| `IEnvironment` | 读写 `HKCU\Environment`（User）与 `HKLM\...\Session Manager\Environment`（Machine）；写后 `SendMessageTimeout(HWND_BROADCAST, WM_SETTINGCHANGE, "Environment")` |
| `IShell.SetKnownFolder` | `SHSetKnownFolderPath(guid, 0, IntPtr.Zero, path)`；先创建目标；返回 HRESULT 转异常；随后 `SHChangeNotify(SHCNE_ASSOCCHANGED)` |
| `IShell.MoveContents` | `Directory.EnumerateFileSystemEntries` 逐项 `Move`；同名跳过并记录；失败抛出并附已移动列表 |
| `IShell.RestartExplorer` | 结束所有 `explorer.exe` 后 `Process.Start("explorer.exe")`；等待 Shell 窗口出现 ≤ 5 s |
| `IShell.PinToQuickAccess` | `Shell.Application`（IDispatch 反射调用，不用 dynamic）：`NameSpace(path).Self.InvokeVerb("pintohome")`；是否已固定用主文件夹命名空间 `{679f85cb-…}` 枚举并检查 `System.Home.IsPinned` |
| `IPower` | 读 `HKLM\...\Control\Power\HibernateEnabled`；写走 `powercfg /hibernate on|off`（唯一受支持方式），检查退出码 |
| `IFileSystem.FilesOlderThan / TryDeleteFile` | 递归枚举跳过重解析点；删除失败（占用）返回 false 不抛出，任务汇总到手动项 |
| `ISystemRestore` | WMI `root\default:SystemRestore.CreateRestorePoint(desc, 0, 100)`；24 小时内已有还原点时系统会跳过，视为成功 |
| `IStorage` | WMI `root\Microsoft\Windows\Storage`：`MSFT_Partition.GetSupportedSize / Resize`、`MSFT_Disk.CreatePartition`、`MSFT_Volume.Format`；所有调用检查 ReturnValue 与 ExtendedStatus |
| `IProcessInfo` | 当前是否管理员；`explorer.exe` 所属用户 SID 与当前进程 SID 比对（HKCU 归属校验） |

所有原生调用集中在 Native 项目，Tasks 只通过接口访问，禁止在任务里直接 P/Invoke。

---

## 8. UI 结构

- `MainWindow`：WPF-UI `FluentWindow` + `NavigationView`（左侧隐藏，顶部步骤条自绘），Mica 背景。
- 页面：`WelcomePage`、`QuestionnairePage`、`PlanPage`、`ExecutePage`、`ReportPage`、`SoftwarePage`、`StoragePage`。
- 每页一个 ViewModel，共享 `SessionState`（Snapshot、Answers、Plan、Result）单例。
- 长操作（探测、执行）在 `Task.Run` 中进行，UI 只订阅 `IProgress`。
- 主题：跟随系统深浅色；强调色取系统。
- 字体：Segoe UI Variable / 微软雅黑回退。
- 动画：仅使用 NavigationView 自带页面过渡与列表项淡入，不做自定义动画。

软件目录 `catalog/software.json`（嵌入资源）：

```json
{ "id": "7zip", "name": "7-Zip", "category": "tools", "url": "https://www.7-zip.org/",
  "install_dir": "7-Zip", "installer": "nsis", "custom_path": true,
  "detect": { "uninstall_name": "7-Zip" }, "usages": ["office","dev","gaming","home"] }
```

`installer` ∈ `msi | nsis | inno | electron_user | msix | other`，决定路径提示文案。

---

## 9. 权限与安全

- 清单 `requireAdministrator`；启动即提权。
- HKCU 归属校验失败则拒绝运行。
- 所有写操作先 journal，后执行；journal 为 JSON Lines，崩溃不丢。
- 不联网（软件入口仅调用 `Process.Start(url)` 交给浏览器）。
- 不收集、不上传任何数据。
- 日志不记录用户名以外的个人信息，不记录环境变量完整值中的凭据类名称（黑名单过滤 `KEY|TOKEN|SECRET|PASS`）。

---

## 10. 错误处理

| 层 | 策略 |
|---|---|
| 任务内 | 抛出 `TaskFailedException(message, inner)`；Runner 捕获并回滚 |
| 原生层 | Win32 错误转 `Win32Exception`，WMI ReturnValue ≠ 0 转 `StorageException(code, extendedStatus)` |
| UI | 全局 `DispatcherUnhandledException` 写日志并提示"已记录到日志"，不静默吞 |
| 崩溃恢复 | 启动时读 `state.json`，`pendingResume=true` 进入 UC-12 |

---

## 11. 构建与发布

- `dotnet build -c Release` 产出 `NewPcSetup.exe`（Costura 嵌入依赖）。
- 版本号：`<Version>` 从 `Directory.Build.props` 统一注入，格式 `0.y.z`。
- `build/publish.ps1`：Release 构建 → 校验单 exe 大小 < 10 MB → 复制到 `artifacts/`。
- 签名：公开发布前 `signtool sign /fd SHA256 /tr <tsa> /td SHA256`。

---

## 12. 测试策略

| 范围 | 方式 |
|---|---|
| Planner、依赖级联、序列化 | xUnit 单测 |
| 每个任务的 Detect / DefaultChecked / IsApplicable | 注入 `FakeRegistry`、`FakeEnvironment`、伪造 Snapshot |
| Apply / Verify / Rollback | 在 Hyper-V 全新 Win11 Home 与 Pro 上跑完整流程；执行前后各跑一次 `build/inspect-machine.ps1` 做快照对比 |
| UI 资源 | `UiSmokeTests`：STA 线程加载 App.xaml 资源并实例化全部视图与主窗口（不显示、不执行任务），捕获缺失的样式/画刷键 |
| UI 交互 | 手工走查 6 屏 + 深浅色 |

---

## 13. 编码规范

- C# 文件作用域命名空间，`Nullable enable`，`TreatWarningsAsErrors` 对 Core/Tasks 开启。
- 任务类命名 `<Module><Name>Task`，文件与 Id 对应。
- 所有用户可见字符串放 `Strings.resx`（首版仅简体中文）。
- 不使用反射注册任务、不使用动态类型、不引入 IoC 容器（手工组装）。

---

## 14. 里程碑

| 里程碑 | 内容 | 完成标准 |
|---|---|---|
| M1 骨架 | 解决方案、Core 模型与接口、Native 注册表/环境变量/已知文件夹、Runner、10 个 ui.* 与 path.temp.user 任务、单测、WPF 壳与 6 页导航 | `dotnet build` 与 `dotnet test` 通过；App 可启动并完成探测→方案预览 |
| M2 路径与探测 | 完整探测、目录骨架、已知文件夹迁移、env.* 全部、C 盘大项扫描、报告页 | 在虚拟机上跑通 UC-06/07/10 |
| M3 磁盘与收尾 | 分区建议与执行、还原点、RunOnce 续跑、GPU 调度、软件入口页 | UC-05/11/12 通过 |
| M4 发布 | Costura 单 exe、签名、发布脚本、Hyper-V 矩阵 | 产物 < 10 MB，Home/Pro 全新机通过 |
