<ui:FluentWindow x:Class="WinHomestead.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WinHomestead.App"
        xmlns:vm="clr-namespace:WinHomestead.App.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="开荒" Width="1100" Height="740" MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
  <Grid TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ui:TitleBar Grid.Row="0" Title="开荒" />

    <!-- 顶部导航：主列表常驻，其余为子页 -->
    <Grid Grid.Row="1" Margin="28,4,28,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <ui:Button Content="{local:Loc Zh='← 返回列表', En='← Back to list'}" Appearance="Secondary" Command="{Binding HomeCommand}" HorizontalAlignment="Left">
        <ui:Button.Style>
          <Style TargetType="ui:Button">
            <Setter Property="Visibility" Value="Visible" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsHome}" Value="True">
                <Setter Property="Visibility" Value="Collapsed" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </ui:Button.Style>
      </ui:Button>
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <ui:Button Content="{Binding ReportLabel}" Appearance="Secondary" Margin="0,0,6,0" Command="{Binding ShowReportCommand}" />
        <ui:Button Content="{local:Loc Zh='软件推荐', En='Software'}" Appearance="Secondary" Margin="0,0,6,0" Command="{Binding ShowSoftwareCommand}" />
        <ui:Button Content="{local:Loc Zh='启动项', En='Startup'}" Appearance="Secondary" Margin="0,0,6,0" Command="{Binding ShowStartupCommand}" />
        <ui:Button Content="{local:Loc Zh='检查项', En='Checks'}" Appearance="Secondary" Command="{Binding ShowCheckupCommand}" />
      </StackPanel>
    </Grid>

    <ContentControl Grid.Row="2" Content="{Binding CurrentPage}" Margin="28,12,28,20" Focusable="False" />
  </Grid>
</ui:FluentWindow>
