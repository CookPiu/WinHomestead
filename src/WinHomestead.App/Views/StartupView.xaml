<UserControl x:Class="WinHomestead.App.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Style="{StaticResource PageTitle}" Text="启动项" />
      <StackPanel Orientation="Horizontal">
        <ui:ProgressRing IsIndeterminate="True" Width="14" Height="14" Margin="0,0,8,0" VerticalAlignment="Center"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBlock Style="{StaticResource PageSubtitle}" Text="{Binding Status}" />
      </StackPanel>
    </StackPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Rows}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <ui:Card Padding="14,10" Margin="0,0,0,6">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                    <Border CornerRadius="8" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center"
                            Background="{DynamicResource ControlFillColorSecondaryBrush}">
                      <TextBlock Text="{Binding SourceName}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </Border>
                  </StackPanel>
                  <TextBlock Text="{Binding Command}" Style="{StaticResource CaptionTextBlockStyle}" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />
                  <TextBlock Text="{Binding Error}" Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,4,0,0"
                             Foreground="{DynamicResource SystemFillColorCautionBrush}"
                             Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
                <ui:Button Grid.Column="1" Content="打开位置" Appearance="Secondary" Margin="14,0,0,0" VerticalAlignment="Center"
                           Command="{Binding OpenLocationCommand}" />
                <ui:ToggleSwitch Grid.Column="2" Margin="14,0,0,0" VerticalAlignment="Center" IsChecked="{Binding Enabled, Mode=TwoWay}" />
              </Grid>
            </ui:Card>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <ui:Button Grid.Row="2" Content="重新读取" Appearance="Secondary" Command="{Binding LoadCommand}" HorizontalAlignment="Right" Margin="0,12,0,0" />
  </Grid>
</UserControl>
