<UserControl x:Class="NewPcSetup.App.Views.SoftwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Style="{StaticResource PageTitle}" Text="软件入口" />
      <TextBlock Style="{StaticResource PageSubtitle}">
        <Run Text="只提供官方网站入口，不内置安装包。安装时选择“自定义安装”，把软件装到 " /><Run Text="{Binding AppsRoot, Mode=OneWay}" FontWeight="SemiBold" /><Run Text=" 下。" />
      </TextBlock>
    </StackPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Groups}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <StackPanel Margin="0,0,0,14">
              <TextBlock Text="{Binding Title}" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="4,0,0,6" />
              <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <ui:Card Padding="14,10" Margin="0,0,0,6">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                          <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <Border CornerRadius="8" Padding="6,1" Margin="8,0,0,0" Background="{DynamicResource SystemFillColorSuccessBackgroundBrush}"
                                    Visibility="{Binding Installed, Converter={StaticResource BooleanToVisibilityConverter}}">
                              <TextBlock Text="已安装" FontSize="11" Foreground="{DynamicResource SystemFillColorSuccessBrush}" />
                            </Border>
                          </StackPanel>
                          <TextBlock Text="{Binding Hint}" Style="{StaticResource Muted}" Margin="0,2,0,0" />
                          <TextBlock Text="{Binding RecommendedPath}" Style="{StaticResource CaptionTextBlockStyle}" FontFamily="Consolas" Margin="0,4,0,0"
                                     Visibility="{Binding HasPath, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                          <ui:Button Content="复制路径" Appearance="Secondary" Command="{Binding CopyPathCommand}" Margin="0,0,6,0"
                                     Visibility="{Binding HasPath, Converter={StaticResource BooleanToVisibilityConverter}}" />
                          <ui:Button Content="官网" Appearance="Primary" Command="{Binding OpenSiteCommand}" />
                        </StackPanel>
                      </Grid>
                    </ui:Card>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <Grid Grid.Row="2" Margin="0,12,0,0">
      <ui:Button Content="返回" Appearance="Secondary" Command="{Binding BackCommand}" HorizontalAlignment="Left" />
      <ui:Button Content="刷新已安装状态" Appearance="Secondary" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" />
    </Grid>
  </Grid>
</UserControl>
