<UserControl x:Class="NewPcSetup.App.Views.ExecuteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Style="{StaticResource PageTitle}" Text="正在执行" />
      <TextBlock Style="{StaticResource PageSubtitle}" Text="{Binding Status}" />
    </StackPanel>

    <ui:Card Grid.Row="1" Padding="16" Margin="0,0,0,12">
      <StackPanel>
        <ProgressBar Minimum="0" Maximum="{Binding Total}" Value="{Binding Completed, Mode=OneWay}" Height="6" />
        <DockPanel Margin="0,8,0,0">
          <TextBlock DockPanel.Dock="Right" Style="{StaticResource Muted}">
            <Run Text="{Binding Completed, Mode=OneWay}" /><Run Text=" / " /><Run Text="{Binding Total, Mode=OneWay}" />
          </TextBlock>
          <TextBlock Text="{Binding CurrentTask}" />
        </DockPanel>
      </StackPanel>
    </ui:Card>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Results}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Grid Margin="4,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock Text="{Binding Name}" />
                <TextBlock Text="{Binding Message}" Style="{StaticResource Muted}" />
              </StackPanel>
              <TextBlock Grid.Column="1" Text="{Binding Outcome}" Margin="12,0,0,0">
                <TextBlock.Style>
                  <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
                    <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding IsOk}" Value="True">
                        <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </TextBlock.Style>
              </TextBlock>
            </Grid>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <Grid Grid.Row="3" Margin="0,12,0,0">
      <ui:Button Content="停止" Appearance="Secondary" Command="{Binding StopCommand}" HorizontalAlignment="Left" />
      <ui:Button Content="查看报告" Appearance="Primary" Command="{Binding ContinueCommand}" HorizontalAlignment="Right" MinWidth="120" />
    </Grid>
  </Grid>
</UserControl>
